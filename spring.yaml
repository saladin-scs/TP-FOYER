pipeline {
    agent any

    environment {
        IMAGE_NAME = "docker.io/khalfaouisaladin/tp-foyer:${BUILD_NUMBER}"
    }

    stages {

        stage('Checkout SCM') {
            steps {
                echo "🔄 Checking out source code..."
                git branch: 'main', url: 'https://github.com/saladin-scs/TP-FOYER.git'
            }
        }

        stage('Build with Maven') {
            steps {
                echo "📦 Building project with Maven..."
                sh 'mvn clean package -DskipTests'
            }
        }

        stage('Build & Push Docker Image') {
            steps {
                echo "🐳 Building and pushing Docker image..."
                withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    sh """
                        docker login -u $DOCKER_USER -p $DOCKER_PASS
                        docker build -t ${IMAGE_NAME} .
                        docker push ${IMAGE_NAME}
                        docker images | grep ${IMAGE_NAME}
                    """
                }
            }
        }

        stage('Run Docker Container') {
            steps {
                echo "🚀 Running Docker container locally..."
                sh """
                    docker stop tp-foyer || true
                    docker rm tp-foyer || true
                    docker run -d --name tp-foyer -p 8081:8080 ${IMAGE_NAME}
                    docker ps | grep tp-foyer
                """
            }
        }
    }

    post {
        success {
            echo "✅ Docker build, push, and run completed successfully!"
        }
        failure {
            echo "❌ Pipeline failed!"
        }
    }
}
